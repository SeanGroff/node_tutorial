const counter = arr => `There are ${ arr.length } elements in this array`;
const adder = (a, b) => `The sum of ${ a } + ${ b } is: ${ a + b }`;
const pi = 3.142;

module.exports = { counter, adder, pi };